'use client'; import { useState } from 'react'; import DashboardLayout from '@/components/layout/DashboardLayout'; import { FileSignature, Send, Eye, Download } from 'lucide-react'; export default function ProvisionalLetterPage() { const [selectedApplicants, setSelectedApplicants] = useState<string[]>([]); const applicants = [ { id: 'APP-2026-001', name: 'Mohamed Kamara', program: 'Bachelor CS', status: 'ready', letterSent: false }, { id: 'APP-2026-002', name: 'Fatmata Sesay', program: 'Master Eng', status: 'ready', letterSent: false }, { id: 'APP-2026-003', name: 'Ibrahim Koroma', program: 'Bachelor BA', status: 'ready', letterSent: true }, ]; const toggleApplicant = (id: string) => { setSelectedApplicants(prev => prev.includes(id) ? prev.filter(x => x !== id) : [...prev, id] ); }; return ( <DashboardLayout> <div className="p-6 bg-gray-50 min-h-screen"> <div className="mb-6"> <div className="flex items-center justify-between"> <div className="flex items-center space-x-3"> <div className="w-12 h-12 bg-blue-100 rounded-lg flex items-center justify-center"> <FileSignature className="w-6 h-6 text-blue-600" /> </div> <div> <h1 className="text-3xl font-bold text-black">Provisional Admission Letters</h1> <p className="text-sm text-black">Generate and send provisional letters</p> </div> </div> {selectedApplicants.length > 0 && ( <button className="px-4 py-2 bg-portal-teal-600 text-white rounded-lg hover:bg-portal-teal-700 flex items-center space-x-2"> <Send className="w-4 h-4" /> <span>Send Letters ({selectedApplicants.length})</span> </button> )} </div> </div> <div className="bg-white rounded-lg shadow-sm border border-gray-200"> <div className="overflow-x-auto"> <table className="w-full"> <thead className="bg-gray-50"> <tr> <th className="px-6 py-3 text-left"> <input type="checkbox" className="rounded text-portal-teal-600" /> </th> <th className="px-6 py-3 text-left text-xs font-medium text-black uppercase">Applicant</th> <th className="px-6 py-3 text-left text-xs font-medium text-black uppercase">Program</th> <th className="px-6 py-3 text-left text-xs font-medium text-black uppercase">Status</th> <th className="px-6 py-3 text-left text-xs font-medium text-black uppercase">Actions</th> </tr> </thead> <tbody className="divide-y divide-gray-200 {applicants.map((app) => ( <tr key={app.id} className="hover:bg-gray-50"> <td className="px-6 py-4"> <input type="checkbox" checked={selectedApplicants.includes(app.id)} onChange={() => toggleApplicant(app.id)} className="rounded text-portal-teal-600" disabled={app.letterSent} /> </td> <td className="px-6 py-4"> <div className="text-sm font-medium text-black">{app.name}</div> <div className="text-sm text-black">{app.id}</div> </td> <td className="px-6 py-4 text-sm text-black">{app.program}</td> <td className="px-6 py-4"> <span className={`px-2 py-1 inline-flex text-xs leading-5 font-semibold rounded-full ${ app.letterSent ? 'bg-green-100 text-green-700' : 'bg-blue-100 text-blue-700' }`}> {app.letterSent ? 'Letter Sent' : 'Ready'} </span> </td> <td className="px-6 py-4 whitespace-nowrap"> <button className="text-portal-teal-600 hover:text-portal-teal-700 mr-3" title="Preview"> <Eye className="w-5 h-5" /> </button> {app.letterSent && ( <button className="text-blue-600 hover:text-blue-700" title="Download"> <Download className="w-5 h-5" /> </button> )} </td> </tr> ))} </tbody> </table> </div> </div> </div> </DashboardLayout> ); } 